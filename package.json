{
  "name": "do-spaces-action",
  "version": "0.0.1",
  "description": "DigitalOcean Spaces Action",
  "main": "bin/src/index.js",
  "scripts": {
    "build": "tsc",
    "dist": "npm run build && ncc build bin/src/index.js --license licenses.txt && rm -rf bin",
    "dev": "nodemon --exec \"npm run build && npm run test\" --watch ./src --ext ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@aws-sdk/client-s3": "^3.379.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.5"
  }
}
